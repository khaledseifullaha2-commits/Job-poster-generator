# Freebuff tool state (databases, logs, run docs) — never commit
.freebuff/

# OS junk
.DS_Store
Thumbs.db

# Logs
*.log
.vercel

# Skills CLI machine-specific symlink dirs (recreated on install; point at absolute local paths)
.claude/
agent/
